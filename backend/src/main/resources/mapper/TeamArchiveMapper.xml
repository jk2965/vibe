<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibe.mapper.TeamArchiveMapper">

    <select id="findByTeam" parameterType="String" resultType="com.vibe.model.TeamArchiveVO">
        SELECT a.id, a.title, a.author_id, a.author_name, a.created_at, a.views, a.team, a.is_required, a.tags,
               (SELECT COUNT(*) FROM vibe.board_comment c WHERE c.board_id = a.id AND (c.is_deleted = 0 OR c.is_deleted IS NULL)) AS comment_count,
               (SELECT COUNT(*) FROM vibe.archive_file f WHERE f.board_id = a.id) AS file_count,
               (SELECT original_name FROM vibe.archive_file WHERE board_id = a.id AND ROWNUM = 1) AS first_file_name
        FROM vibe.team_archive a
        WHERE a.team = #{team}
        ORDER BY a.is_required DESC, a.created_at DESC
    </select>

    <select id="findById" parameterType="String" resultType="com.vibe.model.TeamArchiveVO">
        SELECT id, title, content, author_id, author_name, created_at, views, team, is_required, tags
        FROM vibe.team_archive
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.vibe.model.TeamArchiveVO">
        INSERT INTO vibe.team_archive (id, title, content, author_id, author_name, created_at, views, team, is_required, tags)
        VALUES (#{id}, #{title,jdbcType=VARCHAR}, #{content,jdbcType=VARCHAR},
                #{authorId,jdbcType=VARCHAR}, #{authorName,jdbcType=VARCHAR},
                #{createdAt,jdbcType=VARCHAR}, 0, #{team,jdbcType=VARCHAR}, 0, #{tags,jdbcType=VARCHAR})
    </insert>

    <update id="update" parameterType="com.vibe.model.TeamArchiveVO">
        UPDATE vibe.team_archive
        SET title = #{title,jdbcType=VARCHAR}, content = #{content,jdbcType=VARCHAR}, tags = #{tags,jdbcType=VARCHAR}
        WHERE id = #{id}
    </update>

    <update id="incrementViews" parameterType="String">
        UPDATE vibe.team_archive SET views = views + 1 WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="String">
        DELETE FROM vibe.team_archive WHERE id = #{id}
    </delete>

    <select id="findRequired" resultType="com.vibe.model.TeamArchiveVO">
        SELECT a.id, a.title, a.author_id, a.author_name, a.created_at, a.views, a.team, a.is_required, a.tags,
               (SELECT COUNT(*) FROM vibe.board_comment c WHERE c.board_id = a.id AND (c.is_deleted = 0 OR c.is_deleted IS NULL)) AS comment_count,
               (SELECT COUNT(*) FROM vibe.archive_file f WHERE f.board_id = a.id) AS file_count
        FROM vibe.team_archive a
        WHERE a.is_required = 1
        ORDER BY a.created_at DESC
    </select>

    <update id="setRequired">
        UPDATE vibe.team_archive SET is_required = #{isRequired} WHERE id = #{id}
    </update>

    <select id="findByAuthor" parameterType="String" resultType="com.vibe.model.TeamArchiveVO">
        SELECT a.id, a.title, a.author_id, a.author_name, a.created_at, a.views, a.team, a.is_required, a.tags,
               (SELECT COUNT(*) FROM vibe.board_comment c WHERE c.board_id = a.id AND (c.is_deleted = 0 OR c.is_deleted IS NULL)) AS comment_count,
               (SELECT COUNT(*) FROM vibe.archive_file f WHERE f.board_id = a.id) AS file_count
        FROM vibe.team_archive a
        WHERE a.author_id = #{authorId}
        ORDER BY a.created_at DESC
    </select>

    <!-- 제목 기준 검색: title 컬럼에 keyword 부분 일치 -->
    <select id="searchByTitle" parameterType="String" resultType="com.vibe.model.TeamArchiveVO">
        SELECT a.id, a.title, a.author_id, a.author_name, a.created_at, a.views, a.team, a.is_required, a.tags,
               (SELECT COUNT(*) FROM vibe.board_comment c WHERE c.board_id = a.id AND (c.is_deleted = 0 OR c.is_deleted IS NULL)) AS comment_count,
               (SELECT COUNT(*) FROM vibe.archive_file f WHERE f.board_id = a.id) AS file_count
        FROM vibe.team_archive a
        WHERE a.title LIKE '%' || #{keyword} || '%'
        ORDER BY a.created_at DESC
    </select>

    <!-- 태그 기준 검색: tags 컬럼(쉼표 구분 문자열)에서 keyword와 정확히 일치하는 태그를 포함한 팀 자료실 검색 -->
    <select id="searchByTag" parameterType="String" resultType="com.vibe.model.TeamArchiveVO">
        SELECT a.id, a.title, a.author_id, a.author_name, a.created_at, a.views, a.team, a.is_required, a.tags,
               (SELECT COUNT(*) FROM vibe.board_comment c WHERE c.board_id = a.id AND (c.is_deleted = 0 OR c.is_deleted IS NULL)) AS comment_count,
               (SELECT COUNT(*) FROM vibe.archive_file f WHERE f.board_id = a.id) AS file_count
        FROM vibe.team_archive a
        WHERE a.tags = #{keyword}
           OR a.tags LIKE #{keyword} || ',%'
           OR a.tags LIKE '%,' || #{keyword}
           OR a.tags LIKE '%,' || #{keyword} || ',%'
        ORDER BY a.created_at DESC
    </select>

</mapper>
